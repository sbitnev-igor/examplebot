"""Клавиатуры и расположение кнопок для взаимодействия с пользователями.

Этот модуль содержит функции для создания встроенных клавиатур (InlineKeyboard)
с кнопками, используемыми в различных сценариях взаимодействия бота.
"""

from aiogram.types import InlineKeyboardMarkup, InlineKeyboardButton


def get_main_menu() -> InlineKeyboardMarkup:
    """Создаёт главное меню пользователя.
    
    Возвращает встроенную клавиатуру с основными пункт меню:
    - Профиль: просмотр информации о профиле
    - Баланс: проверка баланса монет
    - Помощь: справка по командам
    
    Возвращает:
        InlineKeyboardMarkup: Объект встроенной клавиатуры с 3 кнопками
    
    Пример использования:
        await message.answer(
            "Выберите действие:",
            reply_markup=get_main_menu()
        )
    """
    buttons = [
        [InlineKeyboardButton(text="📊 Профиль", callback_data="profile")],
        [InlineKeyboardButton(text="💰 Баланс", callback_data="balance")],
        [InlineKeyboardButton(text="❓ Помощь", callback_data="help")],
    ]
    return InlineKeyboardMarkup(inline_keyboard=buttons)
